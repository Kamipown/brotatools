export const green = (v) => `<span class="g">${v}</span>`

export const material = (v) => `<span class="material">${v}</span>`

export const purple = (v) => `<span class="p">${v}</span>`

export const red = (v) => `<span class="r">${v}</span>`

export const small = (v) => `<span class="small">${v}</span>`

export const stat = (v) => `<span class="stat ${v}"></span>`
